{
  "name": "step-3-add-state-actions",
  "version": "1.0.0",
  "description": "In this folder is the start of a `parcel` built `React` application. Let's take the HTML and CSS from the end of Step 1 and convert it into a `React` project!",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.6.0"
  },
  "dependencies": {
    "react": "^18.1.0",
    "react-dom": "^18.1.0"
  }
}
